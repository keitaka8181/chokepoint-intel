# Mapbox public access token. Get one from
# https://account.mapbox.com/access-tokens/ and add a URL restriction.
VITE_MAPBOX_TOKEN=

# AISStream.io API key for the live AIS WebSocket feed.
# If unset, the UI falls back to mock vessel data.
VITE_AISSTREAM_API_KEY=
